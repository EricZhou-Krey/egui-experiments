[package]
name = "navigator"
version = "0.1.0"
edition = "2024"

[dependencies]
egui.workspace = true
eframe.workspace = true
glam.workspace = true
rand.workspace = true

terminal = { path = "../terminal" }
